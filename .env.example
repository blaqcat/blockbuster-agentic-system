# ==============================================================================
# OPTION 1: Google Cloud Application Default Credentials (ADC) / Enterprise OAuth
# Use this when raw API Keys are disallowed by your GCP Organization Policy:
# Run: bash <(curl -sSL https://storage.googleapis.com/cloud-samples-data/adc/setup_adc.sh)
# Then get a token with: gcloud auth print-access-token
# ==============================================================================
VITE_GCP_ACCESS_TOKEN=
GCP_ACCESS_TOKEN=
VITE_GCP_PROJECT_ID=ace-vial-371506
VITE_GCP_REGION=europe-west1

# ==============================================================================
# OPTION 2: Google AI Studio API Key (if API keys are enabled in your project)
# https://aistudio.google.com/app/apikey
# ==============================================================================
VITE_GEMINI_API_KEY=
GEMINI_API_KEY=
